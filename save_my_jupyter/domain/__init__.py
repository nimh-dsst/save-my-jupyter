"""Pure domain layer: dataclasses, enums, value objects, result unions, errors.

Import the specific submodule you need (`domain.config`, `domain.delivery`, …)
rather than this package root — there are no re-exports to keep in sync.
"""

from __future__ import annotations
