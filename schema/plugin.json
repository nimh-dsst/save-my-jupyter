{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "User preferences for Save My Jupyter.",
  "jupyter.lab.setting-deprecated": false,
  "jupyter.lab.toolbars": {
    "Notebook": [
      {
        "caption": "Open Save My Jupyter sidebar",
        "command": "save-my-jupyter:open-panel",
        "label": "SMJ",
        "name": "save-my-jupyter-open-panel",
        "rank": 39
      }
    ]
  },
  "properties": {
    "defaultCommitMode": {
      "default": "ask",
      "description": "Default commit behavior for new snapshot actions.",
      "enum": ["ask", "always", "never"],
      "title": "Default Commit Mode",
      "type": "string"
    },
    "defaultExperimentContext": {
      "deprecated": true,
      "description": "Legacy setting ignored by Save My Jupyter.",
      "title": "Default Experiment Context",
      "type": ["string", "null"]
    },
    "defaultRunLabel": {
      "default": null,
      "description": "Default run label to prefill in the snapshot panel.",
      "title": "Default Run Label",
      "type": ["string", "null"]
    },
    "defaultTags": {
      "default": [],
      "description": "Default snapshot tags.",
      "items": {
        "type": "string"
      },
      "title": "Default Tags",
      "type": "array"
    },
    "rememberCommitChoice": {
      "default": false,
      "description": "Persist the next commit decision as the default commit mode.",
      "title": "Remember Commit Choice",
      "type": "boolean"
    }
  },
  "title": "Save My Jupyter",
  "type": "object"
}
